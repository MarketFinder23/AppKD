import 'package:flutter/material.dart';

class AppColors {
  final white = const Color.fromRGBO(250, 250, 250, 1.0); // Cor clara
  final black = const Color.fromRGBO(15, 15, 15, 1.0); //Cor escura
  final primary = const Color.fromRGBO(36, 74, 89, 1.0); //Cor principal
  final darkRed = const Color.fromRGBO(165, 12, 12, 1.0); //Negativo principal
  final ligthRed = const Color.fromRGBO(182, 91, 91, 1.0); //Negativo secundario
  final darkGreen = const Color.fromRGBO(19, 126, 43, 1.0); //Positivo principal
  final ligthGreen =
      const Color.fromRGBO(85, 168, 103, 1.0); //Positivo secundario

  const AppColors();
}

// -- Palheta de cores para o aplicativo -- \\